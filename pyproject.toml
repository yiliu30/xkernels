[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "marlin-kernels"
version = "0.1.0"
description = "JIT-compiled CUDA MXFP4 W4A16 GEMM kernels"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
